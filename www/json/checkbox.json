{
  "id": "uuid",
  "type": "checkbox",
  "text": "Descripción de la pregunta",
  "instructions": "Instrucciones de la pregunta",
  "minimumSelect": 1,
  "options": [
    {
    "value": 1,
    "text": "Opción 1"
    }
  ],
  "skip_rules": [
    {
    "value": "rule_value",
    "option": "option",
    "default": true
    }
  ],

  "fwdRules" : [
    {
      "type" :  "min|max|value",
      "value":  value,
      "concecuens" : "skip","end","next" //next is the default for all questions except the last one, the behivor at the last one will be "end"
    }
  ],

  "otherOptions": {
    "placeholder": "text",
    "min_length": 12,
    "max_length": 35,
    "regex": "regular_expression"
  }
}
